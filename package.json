{
  "name": "tokscale-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "CLI and macOS app for tracking AI token usage and costs",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cargo build --release -p tokscale-cli && bun run build:cli",
    "build:cli": "bun run --cwd packages/cli build",
    "build:core": "cargo build --release -p tokscale-cli",
    "cli": "./scripts/cli.sh"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
